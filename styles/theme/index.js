/**
 * Theme exports for the ChatERP application
 */

import { colors } from './colors';
import { breakpoints, media } from './breakpoints';
import { typography } from './typography';

// Export the complete theme object
export const theme = {
  colors,
  breakpoints,
  media,
  typography,
};

// Named exports for individual theme components
export { colors, breakpoints, media, typography };
